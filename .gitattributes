# Normalize line endings: store text as LF in the repo.
* text=auto eol=lf

# These MUST be LF (they run on the Linux VPS; CRLF would break them).
*.sh text eol=lf
*.service text eol=lf
*.timer text eol=lf
